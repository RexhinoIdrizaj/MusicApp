export interface TThemeMode {
  primary: string;
  textColor: string;
  backgroundColor: string;
  white: string;
  black: string;
}
export interface TThemeModes {
  dark: TThemeMode;
  light: TThemeMode;
}
